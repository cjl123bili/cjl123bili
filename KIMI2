你是运行在腾讯云轻量服务器（Ubuntu/Debian，有公网 IP）上的编程 agent。N100 家庭主机上有一个已构建好的静态网站（Caddy 托管在 127.0.0.1:8080），需要通过 frp 反向代理到这台云服务器，让我（云服务器）能通过 http://127.0.0.1:8080 访问到它。域名 ICP 备案未通过，所以【绝对不要】监听或放行 ▌ 80/443，也不要配置任何域名/HTTPS。 ▌ 请完成以下事情，每步执行并验证： ▌ 1. 安装 frp 服务端 v0.69.1（必须用 TOML 配置，网上 INI 教程已过时）： ▌    cd /tmp ▌    wget https://github.com/fatedier/frp/releases/download/v0.69.1/frp_0.69.1_linux_amd64.tar.gz ▌    tar -zxvf frp_0.69.1_linux_amd64.tar.gz ▌    sudo mv frp_0.69.1_linux_amd64 /usr/local/frps ▌ 2. 写入 /usr/local/frps/frps.toml，内容如下（一字不差，token 已确定）： ▌    bindPort = 7000 ▌    proxyBindAddr = "127.0.0.1" ▌    auth.method = "token" ▌    auth.token = "yH5m_hYWB5UskPTvF5gXpPlB-6U-RBxt" ▌    注意：proxyBindAddr = "127.0.0.1" 是关键安全配置，穿透出来的端口只绑回环，不能对公网开放。 ▌ 3. 注册 systemd 服务 /etc/systemd/system/frps.service： ▌    [Unit] ▌    Description=frp server ▌    After=network.target ▌ ▌    [Service] ▌    Type=simple ▌    ExecStart=/usr/local/frps/frps -c /usr/local/frps/frps.toml ▌    Restart=on-failure ▌    RestartSec=5 ▌ ▌    [Install] ▌    WantedBy=multi-user.target ▌    然后：sudo systemctl daemon-reload && sudo systemctl enable --now frps ▌ 4. 自测并收集：① systemctl status frps 是否 running；② ss -lntp | grep 7000；③ curl -s ipinfo.io/ip 拿公网 IP；④ 如有 ufw/iptables 则放行 7000/tcp。 ▌ 完成后汇报：公网 IP、frps 状态及关键日志、ss 输出、以及任何与指令不一致的改动。 ▌ （GitHub 下载慢可用 https://mirror.ghproxy.com/ 前缀加速。）
